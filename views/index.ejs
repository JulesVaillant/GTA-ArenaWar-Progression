<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Progress Bar</title>
    <link rel="stylesheet" href="/style.css">
</head>


<body>
    <br><br>
    <div class="container">
            <% for(let i=0; i<RewardsList.length; i++){ %>
                    <div class="item">
                        <label for="progressBar<%= RewardsList[i] %>">
                            <%= RewardsList[i] %>
                        </label><br>
                        <p class="purcentage"><span id="purcentage<%= RewardsList[i] %>">0</span>%</p>
                        <progress id="progressBar<%= RewardsList[i] %>" min="0" max="100" value="0"></progress>
                        <p class="hours"><span id="hours<%= RewardsList[i] %>">0</span>h left</p>
                    </div>
                    <% } %>
    </div>

    <br><br>
    <div class="container">
        <div class="item">
            <label for="inputLevel">Current level</label>
            <input type="number" id="inputLevel" min="0" max="1000" step="1" oninput="updateProgressBars()" value="0">
        </div>
        <div class="item">
            <label for="inputXP">AP per hour?</label>
            <input type="number" id="inputXP" min="0" oninput="updateProgressBars()" value="3000">
        </div>
    </div>

    <br><br>
    <script src="script.js"></script>
</body>

</html>